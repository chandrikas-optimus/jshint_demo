<div class="c-accordion bellows {accordionClass}" {?id}id="{id}"{/id} {?role}role="{role}"{/role} {?manualInit}data-bellows-manual-init{/manualInit}>
    {?.body}
        {.body}
    {:else}
        {#.items}
            {>"components/accordion/accordion-item"/}
        {/.items}
    {/.body}
</div>

{?.button}
    <div class="c-button c--full-width {.btnClass}">{.btnText}</div>
{/.button}
