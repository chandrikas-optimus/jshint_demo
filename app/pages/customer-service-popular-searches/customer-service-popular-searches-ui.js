define(['$', 'global/includes/sidenav/sidenav-ui'], function($, sideNavUI) {
    var customerServicePopularSearchesUI = function() {
        sideNavUI.init();
    };

    return customerServicePopularSearchesUI;
});
