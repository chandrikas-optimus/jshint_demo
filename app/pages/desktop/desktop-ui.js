define(['$'], function($) {
    var desktopUI = function() {
        console.log('desktop UI');
        // Add any scripts you would like to run on the desktop page only here
    };

    return desktopUI;
});
