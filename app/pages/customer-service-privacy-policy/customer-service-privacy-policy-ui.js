define(['$', 'global/includes/sidenav/sidenav-ui'], function($, sideNavUI) {
    var customerServicePrivacyPolicyUI = function() {
        sideNavUI.init();
    };

    return customerServicePrivacyPolicyUI;
});
