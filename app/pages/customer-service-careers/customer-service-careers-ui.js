define(['$', 'global/includes/sidenav/sidenav-ui'], function($, sideNavUI) {
    var customerServiceCareersUI = function() {
        sideNavUI.init();
    };

    return customerServiceCareersUI;
});
