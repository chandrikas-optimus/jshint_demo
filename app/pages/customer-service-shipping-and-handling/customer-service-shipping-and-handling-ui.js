define(['$', 'global/includes/sidenav/sidenav-ui'], function($, sideNavUI) {
    var customerServiceShippingAndHandlingUI = function() {
        sideNavUI.init();
    };

    return customerServiceShippingAndHandlingUI;
});
