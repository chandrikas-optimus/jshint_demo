define(['$'], function($) {
    var accountOrderUI = function() {
        console.log('accountOrder UI');
        // Add any scripts you would like to run on the accountOrder page only here
    };

    return accountOrderUI;
});
